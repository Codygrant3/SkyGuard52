Resume only the existing Unreal Engine 5.8/Blender AAA project at `D:\Skyguard52`. Do not use Three.js, external models, or subagents.

Treat all prior evidence and this Recovery01 offline-design freeze as immutable authority:

- `D:\Skyguard52\Docs\AAA_Review\PHASE4_M01_RECOVERY05_BUILDPLUGIN01_RECOVERY01_OFFLINE_DESIGN_FREEZE.json`
- Required classification: `PASSED_READY_FOR_EXPLICIT_SINGLE_RECOVERY05_BUILDPLUGIN_RECOVERY01_AUTHORIZATION`

Authorize exactly one execution:

`powershell.exe -NoProfile -ExecutionPolicy Bypass -File D:\Skyguard52\Scripts\build_phase4_m01_recovery05_buildplugin01_recovery01_once.ps1 -AuthorizeSingleBuild`

Verify the freeze and every member, the failed Attempt01 freeze and package, all 23 governed plugin records, Recovery04 authority, installed-engine authorities, zero heavy processes, and absence of every Recovery01 future namespace before launch.

Launch bundled .NET 10 exactly once with AutomationTool.dll and the frozen BuildPlugin argument array targeting `D:\SG52R05P02`. Never retry or reuse a namespace. Run no UnrealEditor or Blender.

Normalize only `D:\SG52R05P02\SkyguardRecovery03NativeRecovery05.uplugin`, and only after numeric System.Int32 exit code 0, required-output validation, complete source parity, and package inventory validation. Atomically set only `EnabledByDefault` to Boolean false; preserve all other AutomationTool-generated semantics. Validate the semantic diff and final descriptor.

On any failure, preserve and freeze the attempt as `FAILED_WITH_EVIDENCE`; stop without retry.

On success, validate and hash DLL, PDB, UnrealEditor.modules, normalized descriptor, Build.cs, complete source, semantic diff, logs, process evidence, terminal manifest, package inventory, and all unchanged authorities. Require one supervisor, one bundled-dotnet and one AutomationTool invocation, zero retries, zero engine/Blender launches, no copy-back, and Boolean false disabled-by-default state.

Classify exactly `PASSED_READY_FOR_EXPLICIT_RECOVERY05_RUNTIME_BINDING_DESIGN` or `FAILED_WITH_EVIDENCE`. Do not launch Unreal, integrate, capture, promote, or package the game. Stop after immutable classification and create a separate offline runtime-binding design prompt only if passed.
