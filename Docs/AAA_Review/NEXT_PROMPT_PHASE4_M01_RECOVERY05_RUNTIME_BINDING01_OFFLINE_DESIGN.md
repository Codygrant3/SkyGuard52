Resume only the existing Unreal Engine 5.8/Blender AAA project at `D:\Skyguard52`. Do not use Three.js, external models, or subagents.

Treat all prior evidence and this accepted BuildPlugin Recovery01 freeze as immutable:

- `D:\Skyguard52\Docs\AAA_Review\PHASE4_M01_RECOVERY05_BUILDPLUGIN01_RECOVERY01_ATTEMPT01_TERMINAL_FREEZE.json`
- Required classification: `PASSED_READY_FOR_EXPLICIT_RECOVERY05_RUNTIME_BINDING_DESIGN`

Perform exactly one offline-only Recovery05 Runtime Binding01 design gate. Do not launch UnrealEditor, Blender, BuildPlugin, AutomationTool, UBT, dotnet, a compiler, capture, profiling, integration, promotion, or packaging.

Verify every frozen member, the accepted package `D:\SG52R05P02`, its normalized Boolean-false descriptor, DLL, PDB, `UnrealEditor.modules`, complete source parity, all 23 governed plugin records, the rejected Attempt01 package, Recovery04 authority, and zero heavy processes.

Design a deterministic, recoverable one-shot binding of only these accepted binaries:

- `D:\SG52R05P02\Binaries\Win64\UnrealEditor-SkyguardRecovery03NativeRecovery05.dll`
- its PDB
- `UnrealEditor.modules`

into the active plugin's currently absent fresh directory:

`D:\Skyguard52\Plugins\SkyguardRecovery03NativeRecovery05\Binaries\Win64`

Do not perform the binding during this gate. Do not replace the active descriptor or source. Preserve `EnabledByDefault: false`, module identity, all source hashes, and every quarantined plugin. Require a pre-binding inventory, namespace-absence proof, exact copy allowlist, byte/hash verification, rollback manifest, no overwrite rule, terminal evidence lifecycle, one execution, zero retries, and no Unreal launch.

Prepare an offline verifier and exact-host tests using only temporary non-governed files. Reject existing target files, wrong source hashes, descriptor/module mismatch, missing accepted binaries, partial copies, unexpected files, and rollback ambiguity.

Create a complete binding contract, source/destination inventory, rollback contract, supervisor, verifier, readiness record, immutable offline-design freeze, and a separate one-shot Binding01 execution prompt.

Classify exactly:

- `PASSED_READY_FOR_EXPLICIT_SINGLE_RECOVERY05_RUNTIME_BINDING01_AUTHORIZATION`; or
- `FAILED_WITH_EVIDENCE`.

Do not bind binaries or launch Unreal during this gate. Stop after immutable offline classification and create the execution prompt only if passed.
