Resume only the existing Unreal Engine/Blender AAA project at `D:\Skyguard52`. Do not use the retired Three.js project, external models, or subagents.

Treat the frozen Recovery01 offline design as immutable authority:

- `Docs/AAA_Review/PHASE4_M01_REPRESENTATIVE_VISUAL_ATTEMPT08_RECOVERY01_CONTRACT.json`
- `Saved/Reports/PHASE4_M01_REPRESENTATIVE_VISUAL_ATTEMPT08_RECOVERY01_SOURCE_INVENTORY.json`
- `Saved/Reports/PHASE4_M01_REPRESENTATIVE_VISUAL_ATTEMPT08_RECOVERY01_READINESS.json`
- `Docs/AAA_Review/PHASE4_M01_REPRESENTATIVE_VISUAL_ATTEMPT08_RECOVERY01_FREEZE.json`

I explicitly authorize exactly one Unreal execution of:

`P4.6-M01-REPRESENTATIVE-VISUAL-008-RECOVERY-01`

Do not edit the frozen contract, executor, supervisor, verifier, tests, inherited Attempt08 cameras or rubrics, immutable maps, material, native binding authority, failed Attempt08 evidence, or accepted baselines.

Proceed as follows:

1. Verify every hash in the Recovery01 freeze and inventory.
2. Confirm the failed Attempt08 namespace remains preserved and both Recovery01 namespaces are absent.
3. Confirm no Unreal, Blender, ShaderCompileWorker, AutomationTool, UnrealBuildTool, or other heavy build process is active.
4. Launch exactly once with:
   `powershell.exe -NoProfile -ExecutionPolicy Bypass -File D:\Skyguard52\Scripts\invoke_skyguard_phase4_m01_representative_visual_attempt08_recovery01.ps1`
5. Run no Blender, native build, import, PCG generation, profiling expansion, promotion, integration, or packaging concurrently.
6. Never retry automatically and never reuse a failed namespace.
7. Preserve stdout, stderr, engine log, process ID, actual exit code, timeout status, capture receipt, restoration receipt, frame samples, and every produced PNG as immutable attempt evidence.
8. Require the executor to:
   - load the immutable production map;
   - find exactly one `M01_P4_Landscape_Production`;
   - record its original material identity;
   - bind only the frozen existing validation material in memory;
   - verify a non-null exact material before deferred polling;
   - wait through later editor ticks for two stable sixteen-component shader-ready polls;
   - warm for thirty seconds after readiness;
   - measure for thirty seconds with at least 900 frame samples;
   - capture exactly five static and three temporal 2560×1440 PNGs;
   - restore the original material in `finally`;
   - independently verify restoration identity;
   - never save the world.
9. If the process fails, times out, crashes, loses D3D12 SM6, resumes compilation during measurement, misses a metric or capture, or fails restoration, freeze the evidence and classify `FAILED_WITH_EVIDENCE`.
10. If execution completes, verify every automatic visual, performance, stability, log, hash, capture-count, PNG-dimension, and restoration bound.
11. Inspect all eight PNGs directly at full resolution. Do not accept diagnostic blocks, floating geometry, ungrounded buildings, placeholder repetition, bad exposure, missing shore contact, low-poly hero silhouettes, unstable world elements, clipping, camera-coupled world motion, or unacceptable temporal hitching.
12. Classify the lane exactly as:
    - `PASSED_RECOVERY01_REPRESENTATIVE_VISUAL_ACCEPTED`; or
    - `FAILED_WITH_EVIDENCE`.
13. Do not promote, integrate, package, or claim Mission 1 production acceptance during this gate.
14. Update the Phase 1–8 audit and Mission 1 acceptance matrix only with the exact immutable outcome, paths, hashes, passed gates, remaining gaps, and next executable gate.

Run only this one heavy process. Stop after the immutable postflight classification and audit/matrix update.
