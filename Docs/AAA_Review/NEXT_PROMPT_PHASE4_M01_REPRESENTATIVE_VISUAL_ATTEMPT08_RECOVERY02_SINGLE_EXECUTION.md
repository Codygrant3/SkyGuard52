Resume only the existing Unreal Engine/Blender AAA project at `D:\Skyguard52`. Do not use the retired Three.js project, external models, or subagents.

Treat Recovery01 as immutable and terminal. Treat the following Recovery02 offline-design artifacts as immutable authority:

- `Docs/AAA_Review/PHASE4_M01_REPRESENTATIVE_VISUAL_ATTEMPT08_RECOVERY02_CONTRACT.json`
- `Saved/Reports/PHASE4_M01_REPRESENTATIVE_VISUAL_ATTEMPT08_RECOVERY02_SOURCE_INVENTORY.json`
- `Saved/Reports/PHASE4_M01_REPRESENTATIVE_VISUAL_ATTEMPT08_RECOVERY02_READINESS.json`
- `Docs/AAA_Review/PHASE4_M01_REPRESENTATIVE_VISUAL_ATTEMPT08_RECOVERY02_FREEZE.json`

I explicitly authorize exactly one Unreal execution of:

`P4.6-M01-REPRESENTATIVE-VISUAL-008-RECOVERY-02`

Do not edit any frozen Attempt08, Recovery01, or Recovery02 artifact.

1. Verify every hash and byte count in the Recovery02 freeze and source inventory.
2. Verify the installed editor before launch:
   - `D:\UE_5.8\Engine\Binaries\Win64\UnrealEditor-Cmd.exe`
   - version `++UE5+Release-5.8-CL-56057345`
   - bytes `512952`
   - SHA-256 `0fb325cf215cc800ce260a1890af3f3dc314b8cc6331061d4e7e7246489af2e0`
3. Confirm the Recovery02 attempt, proof, launcher, and execution-preflight namespaces are absent.
4. Confirm no Unreal, Blender, ShaderCompileWorker, AutomationTool, UnrealBuildTool, or other heavy process is active.
5. Launch exactly once with:
   `powershell.exe -NoProfile -ExecutionPolicy Bypass -File D:\Skyguard52\Scripts\invoke_skyguard_phase4_m01_representative_visual_attempt08_recovery02.ps1`
6. Never retry automatically and never reuse a failed namespace.
7. Run no Blender, native build, import, PCG generation, profiling expansion, promotion, integration, or packaging concurrently.
8. Preserve the execution-preflight receipt, stdout, stderr, engine log, process ID, actual exit code, timeout status, capture receipt, restoration receipt, frame samples, and every PNG.
9. Require exactly one governed `M01_P4_Landscape_Production`, the frozen existing validation material bound only in memory, two stable deferred shader-ready polls across sixteen components/resources, thirty seconds of warmup, thirty seconds of measurement, and at least 900 frame samples.
10. Require five static and three temporal 2560×1440 captures using the frozen cameras.
11. Require original material identity recording, restoration in `finally`, independent restoration verification, and no world save.
12. If any preflight, launch, timeout, crash, RHI, shader, capture, metric, log, or restoration condition fails, preserve the evidence and classify `FAILED_WITH_EVIDENCE`.
13. If execution completes, apply every frozen automatic visual, performance, stability, and log bound.
14. Inspect all eight PNGs directly at full resolution. Reject diagnostic blocks, floating geometry, ungrounded buildings, repetition, bad exposure, missing shore contact, low-poly hero silhouettes, unstable world elements, clipping, camera-coupled world motion, or unacceptable temporal hitching.
15. Classify exactly:
    - `PASSED_RECOVERY02_REPRESENTATIVE_VISUAL_ACCEPTED`; or
    - `FAILED_WITH_EVIDENCE`.
16. Create immutable terminal evidence and hashes, then update the Phase 1–8 audit and Mission 1 acceptance matrix with exact paths, hashes, passed gates, unavailable evidence, remaining gaps, and the next executable gate.
17. Do not promote, integrate, package, or claim Mission 1 production acceptance during this gate.

Run only this one heavy process. Stop after terminal classification and audit/matrix updates.
