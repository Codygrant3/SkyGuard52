Resume only the existing Unreal Engine 5.8/Blender AAA project at `D:\Skyguard52`. Do not use Three.js, external models, or subagents.

Treat every prior artifact and this Recovery05 Unreal Proof01 offline-design freeze as immutable:

- `D:\Skyguard52\Docs\AAA_Review\PHASE4_M01_RECOVERY05_UNREAL_PROOF01_OFFLINE_DESIGN_FREEZE.json`
- Required classification: `PASSED_READY_FOR_EXPLICIT_SINGLE_RECOVERY05_UNREAL_PROOF01_AUTHORIZATION`

Authorize exactly one Unreal execution:

`powershell.exe -NoProfile -ExecutionPolicy Bypass -File D:\Skyguard52\Scripts\invoke_phase4_m01_recovery05_unreal_proof01_once.ps1 -AuthorizeSingleUnrealProof`

Before launching, verify the freeze and every member, the accepted Runtime Binding01 freeze, accepted BuildPlugin package, all eight active and eighteen quarantined plugin records, Recovery04 project-build authority, exact UE 5.8 editor, active Recovery05 descriptor/source/bound binaries, production map/material/environment source, frozen cameras/rubrics, zero heavy processes, and absence of every future governed namespace.

Launch exactly one `UnrealEditor-Cmd.exe` process using the frozen 16-argument array. Enable only `SkyguardRecovery03NativeRecovery05`; explicitly disable `SkyguardRecovery03`, `SkyguardRecovery03NativeRecovery01`, `SkyguardRecovery03NativeRecovery04`, Fab, Bridge, EditorTelemetry, RuntimeTelemetry, and EOSShared. Keep the active descriptor disabled by default on disk.

Require D3D12 SM6, the exact production Mission 1 map, exactly one `M01_P4_Landscape_Production`, sixteen components/resources, the frozen existing validation material bound only in memory, two stable deferred tick-driven shader-ready polls, thirty seconds of warmup, thirty seconds of measurement, and at least nine hundred frame samples. Capture exactly five static and three temporal 2560x1440 PNGs from the frozen cameras. Preserve heartbeat, shader, frame, GPU, memory, hitch, crash, timeout, log, capture, terminal, and restoration evidence. Restore the original material in `finally`, independently verify restoration, never save the world, never retry, and never reuse a failed namespace.

Run no Blender, build, import, PCG generation, profiling expansion, promotion, integration, or packaging concurrently.

On failure, preserve and hash the single attempt, classify `FAILED_WITH_EVIDENCE`, and stop without retry.

On successful engine exit, run:

`python D:\Skyguard52\Scripts\verify_phase4_m01_recovery05_unreal_proof01_postflight.py --output D:\Skyguard52\Saved\Reports\PHASE4_M01_RECOVERY05_UNREAL_PROOF01_POSTFLIGHT.json`

Require `PASSED_AUTOMATIC_AWAITING_HUMAN_VISUAL_REVIEW`, then inspect all eight original PNGs directly at full resolution using the frozen checklist. Reject diagnostic blocks, floating or disconnected geometry, ungrounded buildings, placeholder repetition, bad exposure or crushed shadows, missing shoreline contact, low-poly hero silhouettes, unstable clouds/water/foliage/world geometry, clipping, camera-coupled world motion, or unacceptable temporal hitching.

Classify exactly:

- `PASSED_RECOVERY05_REPRESENTATIVE_VISUAL_ACCEPTED`; or
- `FAILED_WITH_EVIDENCE`.

Create immutable terminal evidence and hashes, then update the Phase 1-8 audit and Mission 1 acceptance matrix with exact paths, hashes, passed gates, remaining gaps, and the next executable gate.

Do not integrate, promote, package, or claim Mission 1 or AAA acceptance. Stop after terminal classification and audit updates.
