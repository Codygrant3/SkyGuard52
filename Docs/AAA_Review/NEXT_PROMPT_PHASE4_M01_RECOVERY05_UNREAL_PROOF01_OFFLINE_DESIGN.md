Resume only the existing Unreal Engine 5.8/Blender AAA project at `D:\Skyguard52`. Do not use Three.js, external models, or subagents.

Treat all prior evidence and this accepted Runtime Binding01 freeze as immutable:

- `D:\Skyguard52\Docs\AAA_Review\PHASE4_M01_RECOVERY05_RUNTIME_BINDING01_TERMINAL_FREEZE.json`
- Required classification: `PASSED_READY_FOR_EXPLICIT_RECOVERY05_UNREAL_PROOF_DESIGN`

Perform exactly one offline-only Recovery05 Unreal Proof01 design gate. Do not launch UnrealEditor, Blender, ShaderCompileWorker, AutomationTool, UBT, dotnet, a compiler, capture, profiling, integration, promotion, or packaging.

Verify every frozen member; the active Recovery05 descriptor/source; its newly bound DLL, PDB, and `UnrealEditor.modules`; accepted BuildPlugin package; all quarantined plugin files; Recovery04 project-build authority; Mission 1 environment source/map/material/camera authorities; and zero heavy processes.

Design a fresh, one-shot Recovery05 representative Mission 1 Unreal visual proof. Reconcile and reuse accepted Recovery04 proof logic only where hashes and UE 5.8 compatibility remain valid. The future proof must:

- enable only `SkyguardRecovery03NativeRecovery05` for the governed run and keep it disabled by default on disk;
- disable every legacy duplicate plugin explicitly;
- use the immutable production Mission 1 map and accepted Recovery05 environment source;
- validate D3D12 SM6, exact map/landscape/material/component identities, and the bound DLL hash;
- use deferred tick-driven shader/asset readiness with two stable polls;
- warm for 30 seconds and measure for 30 seconds with at least 900 frame samples;
- capture exactly five static and three temporal 2560x1440 PNGs from frozen rear-gunner and exterior cameras;
- preserve frame, GPU, memory, hitch, crash, timeout, log, heartbeat, capture, and restoration evidence;
- restore any in-memory material binding in `finally`;
- never save the world or mutate disk assets;
- launch exactly once, run one heavy process, never retry, and never reuse a failed namespace;
- retain disabled telemetry/network controls from the accepted prior proof contract.

Create a fresh proof contract, camera/rubric reconciliation, native-runtime compatibility report, launcher/supervisor design, offline verifier, exact-host tests, source inventory, readiness record, immutable offline-design freeze, and a separate one-shot Unreal execution prompt.

Classify exactly:

- `PASSED_READY_FOR_EXPLICIT_SINGLE_RECOVERY05_UNREAL_PROOF01_AUTHORIZATION`; or
- `FAILED_WITH_EVIDENCE`.

Do not launch Unreal or capture images during this design gate. Stop after immutable offline classification and create the execution prompt only if passed.
