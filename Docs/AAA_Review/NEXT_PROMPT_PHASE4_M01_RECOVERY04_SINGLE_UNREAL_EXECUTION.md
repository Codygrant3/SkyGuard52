# Next Gate: Recovery04 Single Unreal Execution

```text
Resume only the existing Unreal Engine/Blender AAA project at `D:\Skyguard52`. Do not use the retired Three.js project, external models, or subagents.

Treat every previous Recovery01–04 source, build, package, preflight, and terminal artifact as immutable.

Treat this runtime-binding freeze as the sole new execution authority:

`D:\Skyguard52\Docs\AAA_Review\PHASE4_M01_RECOVERY04_RUNTIME_BINDING_FREEZE.json`

Verify its externally supplied SHA-256 and every recorded byte count and hash before execution. Require classification:

`PASSED_READY_FOR_EXPLICIT_SINGLE_RECOVERY04_UNREAL_AUTHORIZATION`

I explicitly authorize exactly one Recovery04 Unreal representative Mission 1 visual proof by running:

`powershell.exe -NoProfile -ExecutionPolicy Bypass -File D:\Skyguard52\Scripts\invoke_skyguard_phase4_m01_representative_visual_attempt08_recovery04_once.ps1 -AuthorizeSingleUnrealProof`

Before launch:

1. Verify the binding freeze, accepted native-build freeze, prior terminal preflight freeze, contract, launcher, schemas, verifier, checklist, inventory, readiness record, DLL, PDB, module receipt, source, map, material, cameras, and rubrics.
2. Confirm no Unreal, Blender, ShaderCompileWorker, AutomationTool, UnrealBuildTool, compiler, linker, or other heavy process is active.
3. Confirm all frozen future runtime, proof, launcher, execution-preflight, and terminal-supervisor namespaces remain absent.
4. Confirm the Recovery04 plugin remains disabled by default and its accepted DLL SHA-256 is:
   `2070765a5d44199f7116c2038c97d866b91a509706de73953ead1cad057cb6e3`
5. Confirm the launcher has exactly one Unreal Start-Process path, no retry path, and preserves the immutable inherited Recovery01 switches, tokens, map, and runtime suffix.
6. If any preflight condition fails, do not launch Unreal. Freeze the failure and classify `FAILED_WITH_EVIDENCE`.

Launch the frozen supervisor exactly once. Run no Blender, native build, import, PCG generation, profiling expansion, promotion, integration, or packaging concurrently. Never retry and never reuse a failed namespace.

Preserve the execution preflight, terminal supervisor receipt, stdout, stderr, engine log, process-tree samples, lifecycle heartbeat, frame samples, capture receipt, restoration receipt, native terminal receipt, and all eight PNGs.

If Unreal fails, times out, crashes, returns a null/nonnumeric/nonzero exit code, loses D3D12 SM6, lacks terminal evidence, misses a capture or metric, or fails restoration, preserve the attempt and classify `FAILED_WITH_EVIDENCE`. Do not retry.

If Unreal exits successfully, run:

`python D:\Skyguard52\Scripts\verify_skyguard_phase4_m01_representative_visual_attempt08_recovery04_postflight.py --output D:\Skyguard52\Saved\Reports\PHASE4_M01_RECOVERY04_BINDING01_POSTFLIGHT.json`

Require the automatic postflight classification:

`PASSED_AUTOMATIC_AWAITING_HUMAN_VISUAL_REVIEW`

Then inspect all eight original 2560×1440 PNGs directly at full resolution using:

`D:\Skyguard52\Docs\AAA_Review\PHASE4_M01_RECOVERY04_VISUAL_INSPECTION_CHECKLIST.md`

Reject any failed camera or semantic rejection condition. Automatic checks do not substitute for direct inspection.

Classify exactly:

- `PASSED_RECOVERY04_REPRESENTATIVE_VISUAL_ACCEPTED`; or
- `FAILED_WITH_EVIDENCE`.

Create immutable terminal evidence and hashes. Update the Phase 1–8 audit and Mission 1 acceptance matrix with exact paths, hashes, passed gates, unavailable evidence, remaining gaps, and the next executable gate.

Do not promote, integrate, package, or claim Mission 1 or AAA acceptance. Stop after terminal classification and audit/matrix updates.
```
