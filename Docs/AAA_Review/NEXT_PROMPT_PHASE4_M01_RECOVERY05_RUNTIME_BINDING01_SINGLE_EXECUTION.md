Resume only the existing Unreal Engine 5.8/Blender AAA project at `D:\Skyguard52`. Do not use Three.js, external models, or subagents.

Treat this Runtime Binding01 offline-design freeze as immutable authority:

- `D:\Skyguard52\Docs\AAA_Review\PHASE4_M01_RECOVERY05_RUNTIME_BINDING01_OFFLINE_DESIGN_FREEZE.json`
- Required classification: `PASSED_READY_FOR_EXPLICIT_SINGLE_RECOVERY05_RUNTIME_BINDING01_AUTHORIZATION`

Authorize exactly one binding execution:

`powershell.exe -NoProfile -ExecutionPolicy Bypass -File D:\Skyguard52\Scripts\invoke_phase4_m01_recovery05_runtime_binding01_once.ps1 -AuthorizeSingleBinding`

Before execution, verify the freeze and every member, accepted BuildPlugin package/freeze, all three binary hashes, normalized Boolean-false descriptor, complete source parity, all 23 governed plugin records, zero heavy processes, and absence of the target and binding-attempt namespaces.

Copy exactly the accepted DLL, PDB, and `UnrealEditor.modules` into the fresh active-plugin target `D:\Skyguard52\Plugins\SkyguardRecovery03NativeRecovery05\Binaries\Win64`. Never overwrite, retry, copy any other file, replace the descriptor, alter source/quarantine, or launch a child process, Unreal, or Blender.

Preserve preflight evidence, exact copy allowlist, rollback manifest, terminal manifest, post-binding inventory, and byte/hash parity. On any failure, roll back only files/directories created by this attempt, preserve evidence, classify `FAILED_WITH_EVIDENCE`, and stop without retry.

On success require exactly three target files, exact accepted hashes, unchanged descriptor/source/quarantine, one binding execution, three copies, zero retries, and zero engine/Blender launches. Freeze the result and classify exactly:

- `PASSED_READY_FOR_EXPLICIT_RECOVERY05_UNREAL_PROOF_DESIGN`; or
- `FAILED_WITH_EVIDENCE`.

Do not launch Unreal, capture, profile, integrate, promote, or package. Only if passed, create a separate offline Unreal-proof design prompt and stop.
