# Next Gate: Recovery04 Single Unreal Representative Visual Proof

```text
Resume only the existing Unreal Engine/Blender AAA project at `D:\Skyguard52`. Do not use the retired Three.js project, external models, or subagents.

Treat every prior Recovery01–04 source, supervisor, build attempt, package, and terminal artifact as immutable and terminal.

Treat the Recovery04 native-build freeze as the sole new authority:

- `D:\Skyguard52\Docs\AAA_Review\PHASE4_M01_RECOVERY03_NATIVE_BUILD_RECOVERY04_NATIVE_BUILD_FREEZE.json`
- Verify its recorded SHA-256 before any execution.
- Required classification:
  `PASSED_READY_FOR_EXPLICIT_SINGLE_RECOVERY04_UNREAL_AUTHORIZATION`

I explicitly authorize exactly one governed Unreal execution of the Recovery04 representative Mission 1 visual proof.

Before launch:

1. Verify every hash and byte count in the Recovery04 native-build freeze, source/binary inventory, source-parity record, build acceptance record, and readiness record.
2. Verify the Recovery04 source plugin remains disabled by default and its rebound DLL, PDB, and module receipt exactly match the accepted package at `D:\SG52R03B05`.
3. Preserve all earlier failed attempts, packages, runtime namespaces, and accepted baselines unchanged.
4. Confirm no Unreal, Blender, ShaderCompileWorker, AutomationTool, UnrealBuildTool, compiler, linker, or other heavy process is active.
5. Confirm the fresh Recovery04 runtime-attempt, proof, launcher, and execution-preflight namespaces are absent.
6. Validate the frozen runtime contract, launcher, native lifecycle, cameras, rubrics, hashes, network controls, and restoration requirements offline.
7. If any preflight condition fails, do not launch Unreal; freeze the failure and classify `FAILED_WITH_EVIDENCE`.

Launch exactly one UnrealEditor-Cmd process using the accepted Recovery04 rebound plugin and the previously frozen representative-visual contract. Run no Blender, build, import, PCG generation, profiling expansion, promotion, integration, or packaging concurrently. Never retry automatically and never reuse a failed namespace.

Require the governed native lifecycle to:

- remain inert unless the exact one-shot authorization is present;
- validate D3D12 SM6, the immutable production map, exactly one governed landscape, its original material identity, all sixteen components/resources, the frozen existing validation material, cameras, and output namespaces;
- bind only the frozen material in memory;
- wait through persistent later editor ticks for two stable shader-ready polls;
- reset readiness if compilation resumes;
- warm for thirty seconds after readiness;
- measure for thirty seconds with at least 900 frame samples;
- capture exactly five static and three temporal 2560x1440 PNGs;
- record frame, GPU, working-set, GPU-memory, heartbeat, capture, log, and stability evidence;
- restore the original material in `finally`;
- independently verify restoration identity;
- write the terminal receipt before shutdown;
- never save the world or mutate a disk asset.

Preserve stdout, stderr, engine log, PID and process tree, actual numeric exit code and type, timeout state, heartbeat, frame samples, capture receipt, restoration receipt, terminal receipt, and every PNG.

If the process fails, times out, crashes, loses D3D12 SM6, resumes compilation during measurement, misses any metric/capture, fails restoration, or lacks terminal evidence, preserve the attempt and classify exactly `FAILED_WITH_EVIDENCE`. Do not retry.

If execution completes, validate every frozen automatic visual, performance, stability, log, hash, capture-count, PNG-dimension, and restoration bound. Inspect all eight PNGs directly at full resolution. Reject diagnostic blocks, floating geometry, ungrounded buildings, placeholder repetition, bad exposure, missing shore contact, low-poly hero silhouettes, unstable clouds/water/foliage/world geometry, clipping, camera-coupled world motion, or unacceptable temporal hitching.

Classify exactly:

- `PASSED_RECOVERY04_REPRESENTATIVE_VISUAL_ACCEPTED`; or
- `FAILED_WITH_EVIDENCE`.

Create immutable terminal evidence and hashes, then update the Phase 1–8 audit and Mission 1 acceptance matrix with exact paths, hashes, completed gates, remaining gaps, and the next executable gate.

Do not promote, integrate, package, or claim Mission 1 or AAA acceptance during this gate. Stop after terminal classification and audit/matrix updates.
```
