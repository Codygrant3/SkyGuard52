Resume only the existing Unreal Engine 5.8/Blender AAA project at `D:\Skyguard52`. Do not use Three.js, external models, or subagents.

Treat this Recovery01 offline-design freeze as the sole new execution authority:

- file:
  `D:\Skyguard52\Docs\AAA_Review\GATE7_COMBAT_ASSET_REFINEMENT_CYCLE03_RAIL_COUPON_RECOVERY01_OFFLINE_DESIGN_FREEZE.json`
- bytes: `5747`
- SHA-256:
  `e349fc9e79bad018663d95d2499f4ad1d68ad30825d9f65f1a11432687c659b2`
- classification:
  `PASSED_READY_FOR_EXPLICIT_SINGLE_RAIL_COUPON_RECOVERY01_BLENDER_AUTHORIZATION`

Treat every historical Cycle03, Recovery03, and failed Attempt01 artifact as immutable. Never rerun or reuse the failed Attempt01 binding.

## Authorization

Authorize exactly one Recovery01 Blender execution by running:

`powershell.exe -NoProfile -ExecutionPolicy Bypass -File D:\Skyguard52\Scripts\invoke_gate7_combat_asset_refinement_cycle03_rail_coupon_recovery01_once.ps1 -AuthorizeSingleBlender`

The only authorized asset is:

`PROVISIONAL_MIL_STD_1913_VALIDATION_COUPON`

Do not refine the rifle, missile, hand, or Shahed. Do not launch UnrealEditor, a build, importer, integration, promotion, profiling, gameplay, or packaging.

## Preflight

Before creating a namespace or launching Blender:

1. Verify the Recovery01 freeze and every recorded member.
2. Verify the one-way source inventory:
   - `D:\Skyguard52\Saved\Reports\GATE7_COMBAT_ASSET_REFINEMENT_CYCLE03_RAIL_COUPON_RECOVERY01_SOURCE_INVENTORY.json`
   - bytes: `5529`
   - SHA-256:
     `5790c54a59591670a3c4af0e7a643ffa1d83589420bb507d20b068eb37e0f443`
3. Verify the execution contract:
   - bytes: `6798`
   - SHA-256:
     `f57e5251344e8eda6b8ab6a91a50bf745d9b8ca2b5316558acb0438253b520a0`
4. Verify the Recovery01 generator:
   - bytes: `1715`
   - SHA-256:
     `1a82efe7cdd7c412cf3d8d200558ac97801c7afbfbcb356851ce5d65b4f2843e`
5. Verify the supervisor:
   - bytes: `16781`
   - SHA-256:
     `6fb9926ddf01050ca3f556dd0bf848c1d498280be8bb07b640db5914ee376c2d`
6. Rerun:
   `python D:\Skyguard52\Scripts\verify_gate7_combat_asset_refinement_cycle03_rail_coupon_recovery01_offline.py`
7. Require verifier result `PASS`.
8. Confirm exactly one supervisor `Start-Process`, Blender only, with no retry path.
9. Confirm no Blender production executable, UnrealEditor, ShaderCompileWorker, AutomationTool, UnrealBuildTool, compiler, linker, or other heavy process is active.
10. Confirm these namespaces are absent:
    - `D:\Skyguard52\Saved\BuildAttempts\GATE7_COMBAT_ASSET_REFINEMENT_CYCLE03_RAIL_COUPON_RECOVERY01\attempt_01`
    - `D:\Skyguard52\Blender\GATE7_COMBAT_ASSET_REFINEMENT_CYCLE03_RAIL_COUPON_RECOVERY01_ATTEMPT01`
    - Recovery01 terminal manifest;
    - Recovery01 emergency receipt.

If preflight fails, launch nothing. Preserve evidence and classify `FAILED_WITH_EVIDENCE`.

## Single Blender execution

Launch the frozen supervisor exactly once.

Run only one heavy process. Never retry automatically and never reuse a failed namespace.

Preserve:

- exact executable and arguments;
- PID and process-tree samples;
- start/end timestamps;
- timeout state;
- numeric exit code and type;
- stdout and stderr;
- preflight receipt;
- terminal supervisor manifest;
- every generated artifact and hash.

## Dimensional authority

Preserve:

- coupon length: `0.12 m`;
- top width: `0.021209 m`;
- minimum profile height: `0.009322 m`;
- dovetail width: `0.018999 m`;
- groove width: `0.005232 m`;
- pitch: `0.010008 m`.

Keep the asset a dimensional validation coupon. Do not claim it is a complete weapon component and do not invent manufacturer or weapon identity.

## Required outputs

Require:

1. exactly one governed `.blend`;
2. exactly one GLB;
3. `SOCKET_Rail_Origin`;
4. `UCX_RailCoupon_ProfilePrism`;
5. dimension receipt with all bounds passing;
6. GLB structure receipt;
7. exactly eleven 2048×2048 Cycle03 review renders;
8. daylight, overcast, night, wet, and cockpit-light coverage;
9. front, side, top, rear, section, hero, and detail cameras;
10. artifact inventory and hashes;
11. terminal receipt and supervisor manifest.

## Visual review

If automatic validation passes, inspect every original PNG directly at full resolution.

Reject:

- broken or implausible rail silhouette;
- lost dimensional profile;
- visible mesh gaps, intersections, shading errors, or clipping;
- diagnostic or unsupported identity markings;
- material failure under any required lighting set;
- missing socket/collision separation;
- presentation as a complete weapon component.

## Classification

Classify exactly:

- `PASSED_READY_FOR_NEXT_COMBAT_ASSET_REFINEMENT_STEP`; or
- `FAILED_WITH_EVIDENCE`.

On failure, preserve the attempt unchanged and stop without retry.

On success, create immutable terminal evidence, update Gate 7 and Phase 1–8 audits, and identify the next executable asset step as the bounded AR/M4-family silhouette and ADS refinement design/execution gate.

Do not import into Unreal or replace any runtime asset.

Stop after terminal classification and audit updates.
