Resume only the existing Unreal Engine 5.8/Blender AAA project at `D:\Skyguard52`. Do not use Three.js, external models, or subagents.

Treat the accepted Recovery03 provisional-blockout freeze and the Cycle03 offline-design freeze as immutable. Treat the failed Rail Coupon Attempt01 terminal freeze as immutable and terminal.

## Authorization

Perform exactly one offline-only Rail Coupon Recovery01 authority-reconciliation design gate.

Do not launch Blender, Blender MCP, UnrealEditor, ShaderCompileWorker, AutomationTool, UnrealBuildTool, a compiler, renderer, exporter, importer, integration, promotion, or packaging.

Do not modify:

- the Cycle03 offline-design freeze;
- its mismatched source-inventory member;
- the accepted Recovery03 blockout freeze or namespace;
- the failed Attempt01 terminal manifest;
- the Attempt01 generator or supervisor;
- any accepted Unreal runtime asset.

## Objective

Reconcile why the Cycle03 freeze records:

- 8,366 bytes;
- SHA-256 `be8f4582bdd994c07b8d5da892d1bfdc4c43216cb92b621b175cfe4f6a0cc89f`

for:

`D:\Skyguard52\Saved\Reports\GATE7_COMBAT_ASSET_REFINEMENT_CYCLE03_SOURCE_INVENTORY.json`

while the observed immutable file is:

- 9,333 bytes;
- SHA-256 `1030ebfcc54bacc4d1e2cf7dcd26b9da0f4b90f5ecb702772ab75e5d5eaadafc`.

Determine whether this is a freeze/final-write ordering race or another bounded provenance defect. Preserve both states as evidence; do not silently bless, restore, or overwrite either.

## Required work

1. Verify the Cycle03 freeze, Recovery03 freeze, failed Attempt01 terminal freeze, terminal supervisor manifest, generator, supervisor, and every still-valid Cycle03 member.
2. Create a member-by-member reconciliation identifying the single mismatch and confirming whether any additional drift exists.
3. Compare the observed source inventory semantically with the files actually present at Cycle03 design completion.
4. Create a fresh Recovery01 source inventory and authority contract in new namespaces.
5. Preserve accepted rail dimensions, camera/lighting contract, visual rubric, socket, collision, scope, and no-Unreal rules unchanged.
6. Reserve fresh Recovery01 attempt/output/terminal namespaces; do not create them during this offline gate.
7. Derive a new Recovery01 generator/supervisor only if necessary; do not modify Attempt01 files.
8. Validate Python and Windows PowerShell 5.1 syntax, exact one-Blender-launch structure, zero retry paths, namespace absence, process state, and focused offline tests.
9. Freeze the complete Recovery01 package and create a separate exact one-shot Blender authorization prompt.

## Classification

Classify exactly:

- `PASSED_READY_FOR_EXPLICIT_SINGLE_RAIL_COUPON_RECOVERY01_BLENDER_AUTHORIZATION`; or
- `FAILED_WITH_EVIDENCE`.

Do not execute Blender during this gate. Stop after immutable offline classification and create the separate execution prompt only if passed.
