{
  "name": "skyguard-unreal-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only-first MCP bridge for the Skyguard 52 Unreal Engine project",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "start": "node dist/src/server.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
